# 근무자동화 프로젝트 작업 규칙

## Supabase 데이터 보호: 위험 작업 전에 백업 필수

사용자 요청에 따라 이 저장소에서 코딩할 때 다음 규칙을 적용한다.

1. 작업 시작 시 Supabase 데이터 삭제, 누락, 덮어쓰기 가능성을 먼저 확인한다. SQL/마이그레이션, 테이블·컬럼 변경, 삭제·초기화, 일괄 수정, 데이터 변환·이관·복구, 저장·동기화·자동 정리 로직 변경을 포함한다. 삭제 쿼리가 없어도 불완전한 조회 결과를 다시 저장하거나 잘못된 upsert로 기존 값을 덮어쓸 수 있으면 위험 작업이다.
2. 백업이 필요한 시점은 운영 데이터를 변경하는 명령·마이그레이션·복구·실데이터 테스트를 실행하기 전이다. 현재 운영 프로젝트와 영향 테이블을 확인하고 읽기 전용 방식으로 새 백업을 생성하며, 관련 테이블과 참조 관계를 함께 보관한다. 백업 이후 데이터가 변경되었으면 실행 직전에 다시 백업한다. 운영 DB 변경을 실행하지 않는 코드 작성·테스트·커밋·푸시는 백업 없이 진행한다. 저장 로직을 수정한다는 이유만으로 코드 작업이나 푸시를 보류하지 않는다. 단, 푸시·배포가 자동 마이그레이션이나 데이터 변경 작업을 실행하는 경우에는 먼저 백업한다.
3. 백업은 브라우저 저장소에만 두지 않고 이 저장소의 `supabase-backups/` 또는 `backups/` 아래에 프로젝트 식별자와 시각이 포함된 고유 파일로 저장한다. 기존 파일을 덮어쓰거나 자동 삭제하지 않는다. 파일 권한은 소유자만 읽고 쓰도록 제한하고, 백업 원문·환자 정보·인증키를 로그나 응답에 출력하거나 Git에 커밋하지 않는다.
4. 백업 생성 성공 메시지만으로 완료 처리하지 않는다. 파일 재읽기, 파싱, 대상 프로젝트, 생성 시각, 모든 영향 테이블 포함 여부, 실제 원본과 테이블별 행 수, 조회 오류·페이지 누락 여부를 검증한다. RLS나 권한 제한으로 일부 행만 조회된 백업, 원인을 확인하지 못한 0행 결과, 오류가 포함된 부분 백업은 사용하지 않는다. 여러 테이블의 일관성이 필요한 작업은 일관된 스냅샷을 확보하거나 쓰기를 통제할 수 있을 때만 진행한다.
5. 데이터뿐 아니라 스키마·정책·함수·트리거 등을 변경하면 해당 정의와 복구에 필요한 메타데이터도 보관한다. Auth·Storage 등이 영향 범위이면 해당 자원도 별도로 보관한다. 앱의 고정 테이블 목록만으로 전체 Supabase 프로젝트가 백업되었다고 간주하지 않는다.
6. 백업 경로, 생성 시각, 영향 테이블과 행 수, 검증 결과, 복구 방법을 기록한 뒤 위험 작업을 진행한다. 기존 백업 형식과 절차는 `docs/supabase-local-backup.md`, `src/lib/supabaseBackupUtils.js`, `scripts/restore-supabase-backup.mjs`를 확인한다. 복구 dry-run은 파일 점검일 뿐 완전한 복구 가능성 검증은 아니다. 파괴적 스키마 변경이나 대량 이관은 격리 DB에서 복원 검증을 먼저 한다. 운영 DB에서 복구 테스트를 하지 않는다.
7. 백업 생성·검증이 실패하거나 전체 영향 범위에 대한 읽기 권한이 없으면 운영 데이터를 변경하는 작업의 실행만 중단하고 이유와 필요한 정보만 사용자에게 알린다. 운영 데이터를 변경하지 않는 코드 작업·커밋·푸시는 계속 진행한다. 테스트 통과, Git 커밋, 과거 백업의 존재를 실제 DB 변경 전 새 백업의 대체물로 삼지 않는다. 백업이 있어도 요청 범위를 벗어난 삭제·초기화·복구 권한이 생기는 것은 아니다.
8. 실제 DB 변경 작업 후 백업 전후 행 수와 관련 데이터의 정합성을 확인한다. 예상하지 못한 누락이 발견되면 추가 쓰기를 중단하고 복구 계획을 검토한다.

글자 크기·색상·여백 등 데이터 읽기/쓰기 동작에 영향을 주지 않는 순수 표시 변경과 읽기 전용 조사는 백업 없이 진행한다.

이 파일은 코딩 에이전트의 작업 규칙이다. Supabase 서버의 자동 백업, 주기 작업, SQL 실행 차단 장치를 설치한 것은 아니다.
